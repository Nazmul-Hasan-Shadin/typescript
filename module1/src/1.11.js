{
    // ternary operator || optional chaining || nullish coalescing operator
    var age = 15;
    if (age >= 18) {
        console.log('adult');
    }
    else {
        console.log('not adult');
    }
}
